# 如何向 SaigeAPPs 添加应用

SaigeAPPs 的应用卡片由 `content/apps.json` 自动生成。以后新增应用时，不需要修改页面代码，只需准备一张截图并运行新增命令。

## 1. 准备应用截图

为了让所有卡片看起来整齐，请统一使用以下规范：

- 尺寸：推荐 `1440 × 900 px`（16:10）。
- 格式：PNG，文件名固定为 `cover.png`。
- 内容：使用应用的干净初始页面，尽量选择中文界面。
- 隐私：不要出现真实项目名称、客户信息、文件路径或其他私人数据。
- 浏览器：不要截入地址栏、书签栏、鼠标指针或系统桌面。
- 构图：保留应用标题和核心操作区，避免把很长的整页缩小到一张图里。
- 大小：建议控制在 1.5 MB 以内；在不明显影响清晰度的情况下可压缩图片。

先确定一个应用 `slug`。它是应用的唯一英文短名，只能包含小写字母、数字和短横线，例如 `project-exporter`。

将截图放到：

```text
public/apps/<slug>/cover.png
```

例如：

```text
public/apps/project-exporter/cover.png
```

## 2. 运行新增命令

在项目目录运行：

```bash
npm run app:add
```

根据提示填写：

1. `slug` 和应用网址；
2. 中文、英文、韩文名称；
3. 中文、英文、韩文简介；
4. 状态、分类和三种语言的标签；
5. 卡片强调色、发布日期和是否隐藏。

三种语言的标签数量需要一致，建议每种语言填写 3 个标签。命令会自动检查：

- `slug` 格式是否正确；
- `slug` 或网址是否已经存在；
- 网址是否为有效的 HTTP/HTTPS 地址；
- 状态、日期和颜色格式是否正确；
- 固定位置是否已经存在 `cover.png`。

只有检查全部通过后，应用才会写入 `content/apps.json`。如果截图尚未放好，命令会创建对应目录并暂停；复制截图后按 Enter 即可继续。

## 3. 预览并检查

新增完成后，先在本地预览：

```bash
npm run dev
```

重点检查：

- 三种语言的名称、简介和标签是否自然；
- 截图主体在桌面端和手机端是否清晰；
- 卡片链接是否打开正确的应用；
- 强调色是否和应用自身界面协调。

发布前运行：

```bash
npm run build
```

## 字段说明

`content/apps.json` 是应用列表的唯一数据源。常用字段如下：

| 字段 | 用途 |
| --- | --- |
| `slug` | 应用唯一标识，同时决定截图目录 |
| `name` | 中、英、韩三语名称 |
| `description` | 中、英、韩三语一句话简介 |
| `url` | 点击卡片后打开的应用地址 |
| `cover` / `coverAlt` | 截图地址和三语无障碍说明 |
| `status` | `stable`、`beta`、`experimental`、`maintenance` 或 `archived` |
| `category` | 稳定的分类 ID，例如 `saigevision` |
| `tags` | 中、英、韩三语标签 |
| `accent` | 卡片的主强调色和浅色背景 |
| `privacy` | 处理方式标识；纯浏览器本地处理可写 `local` |
| `publishedAt` | `YYYY-MM-DD` 格式的发布日期 |
| `hidden` | 设为 `true` 时不在网站展示 |

## 更新、暂时隐藏或归档

- 更新截图：直接替换对应目录中的 `cover.png`，文件名和路径不要改变。
- 更新文案：编辑 `content/apps.json` 中对应应用的三语字段。
- 暂时下线：将 `hidden` 改为 `true`，保留数据以便以后恢复。
- 停止维护但仍想保留入口：将 `status` 改为 `archived`。
- 永久删除：确认不再需要后，再同时删除 JSON 条目和对应截图目录。

直接编辑 JSON 后，务必运行 `npm run build`；少一个逗号或引号都会导致构建检查失败。
